
// we need to test this type of code
// createApp(dpapp => props => ReactDOM.render(
//   <AppFrame {...props}>
//     <App dpapp={dpapp}/>
//   </AppFrame>,
//   document.getElementById('deskpro-app')
// ));
